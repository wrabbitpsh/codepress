# PRD: 부산 AI·DX 지원사업 문의 포털

## 1. 프로덕트 개요

**서비스명:** 부산 AI·DX 지원사업 문의 포털  
**운영주체:** 부산정보산업진흥원 (BIPA)  
**목적:** 지역기업·시민이 부산정보산업진흥원의 지원사업에 관한 문의를 등록하고 열람할 수 있는 단일 창구 제공  
**제약:** 교육·시범용 포털 (별도 백엔드 서버 없음, 클라이언트 단 데이터 저장)

---

## 2. 타겟 사용자

| 사용자 유형 | 설명 |
|---|---|
| 일반 시민·기업 담당자 | 지원사업 정보를 모르는 신규 문의자 |
| 스타트업·창업 준비자 | 입주·창업 지원 관련 문의자 |
| 관리자 (담당 직원) | 문의 목록 열람, 상태 관리, 답변 등록, 엑셀 내보내기/가져오기를 통해 오프라인 관리 |

---

## 3. 핵심 기능 요구사항

### 3.1 문의 등록 폼

- 사용자는 아래 4개 필드를 모두 입력하여 문의를 등록한다.

| 필드 | 타입 | 필수 | 선택지 |
|---|---|---|---|
| 카테고리 | `<select>` | ✅ | AI·DX 솔루션 도입 / 데이터·클라우드 바우처 / 교육·인재양성 신청 / 스타트업 입주·창업 / 게임·콘텐츠 제작 지원 / 해외진출 / 기타 |
| 제목 | `<input text>` | ✅ | - |
| 이름 | `<input text>` | ✅ | - |
| 내용 | `<textarea>` | ✅ | - |

- 하나 이상의 필드가 비어 있으면 **인라인 오류 메시지**("모든 항목을 입력해주세요.")를 폼 하단에 표시한다.
- 등록 성공 시 폼 초기화, 카테고리 필터를 "전체"로 리셋, 목록 즉시 갱신.
- 입력 중 오류 메시지는 자동으로 숨김 처리.

### 3.2 문의 목록

- 등록 순서의 역순(최신순)으로 목록을 표시한다.
- 각 카드에 표시되는 정보:
  - 카테고리 뱃지 (파란 pill 형태)
  - 문의 상태 뱃지 (접수됨/처리중/완료, 색상 구분)
  - 등록 경과 시간 (상대 시각, 하단 참고)
  - 제목 (굵게)
  - 작성자 이름
  - 내용 (줄바꿈 보존)
  - 관리자 답변 (답변이 있는 경우 일반 사용자에게도 읽기 전용으로 표시)
  - 삭제 버튼 (관리자 모드에서만 표시)

#### 상대 시각 표시 규칙

| 조건 | 표시 |
|---|---|
| 10초 미만 | 방금 |
| 10초 이상 ~ 60초 미만 | N초 전 |
| 1분 이상 ~ 1시간 미만 | N분 전 |
| 1시간 이상 ~ 24시간 미만 | N시간 전 |
| 24시간 이상 | N일 전 |

- 상대 시각은 **30초마다 자동 갱신**한다.

### 3.3 카테고리 필터

- 목록 상단에 "전체" + 7개 카테고리 필터 칩(pill 버튼)을 렌더링한다.
- 활성 필터는 파란 배경으로 하이라이트, 비활성은 테두리형.
- 필터 선택 시 해당 카테고리 문의만 표시, 건수 뱃지 갱신.
- 전체 건수 표시: "총 N건" (목록 우상단).

### 3.4 문의 삭제

- **관리자 모드에서만** 카드 우측 삭제 버튼 표시.
- 삭제 버튼 클릭 시 confirm 다이얼로그 표시.
- 확인 시 해당 문의 즉시 삭제 후 목록·건수 갱신.

### 3.5 엑셀 내보내기

- **관리자 모드에서만** 노출.
- 현재 **전체** 문의 목록을 최신순으로 정렬하여 `.xlsx` 파일로 내보낸다.
- 파일명 형식: `문의목록_YYYY-MM-DD.xlsx`
- 열 구성 및 너비:

| 열 이름 | 최대 너비(문자) |
|---|---|
| 번호 | 8 |
| 카테고리 | 20 |
| 제목 | 40 |
| 이름 | 10 |
| 내용 | 60 |
| 상태 | 12 |
| 답변 | 60 |
| 등록일시 | 20 |

- 등록일시는 한국어 로캘 포맷(`ko-KR`)으로 표시.

### 3.6 엑셀 가져오기

- **관리자 모드에서만** 노출.
- `.xlsx` / `.xls` 파일을 선택하여 문의를 일괄 등록한다.
- 파일의 첫 번째 시트를 파싱.
- **필수 열:** 카테고리, 제목, 이름, 내용
  - 누락 시 어느 열이 없는지 명시한 오류 alert 표시 후 중단.
- 제목 또는 이름이 빈 행은 건너뜀.
- 가져온 항목은 기존 목록 앞에 추가(최신 항목이 위로).
- 완료 후 "N건을 가져왔습니다." alert 표시.
- 파일 입력값 초기화 (동일 파일 재선택 가능).

### 3.7 관리자 인증 (신규)

- 헤더 우측에 "관리자" 텍스트 버튼을 항상 표시한다.
- 버튼 클릭 시 비밀번호 입력 모달을 표시한다.
- 비밀번호 일치 시 관리자 모드로 전환, `sessionStorage`에 인증 상태 저장.
  - 탭·브라우저 종료 시 자동 로그아웃.
- 비밀번호 불일치 시 모달 내 인라인 오류 메시지 표시.
- 관리자 모드 진입 후 헤더의 "관리자" 버튼은 "로그아웃" 버튼으로 교체.
- 비밀번호: `bipa2025` (교육·시범용, 클라이언트 소스에 하드코딩).

> **보안 주의:** 클라이언트 사이드 인증은 소스 코드 열람으로 우회 가능하므로 운영 보안 목적으로 사용하지 않는다.

### 3.8 문의 상태 관리 (신규)

- 각 문의는 **접수됨 / 처리중 / 완료** 세 가지 상태를 가진다.
- 기본값: `접수됨`.
- **관리자 모드**에서 카드 내 `<select>`로 상태 변경 가능, 변경 즉시 저장.
- 일반 사용자 뷰에서도 상태 뱃지를 읽기 전용으로 표시한다.

| 상태 | 뱃지 색상 |
|---|---|
| 접수됨 | 회색 (`#F3F4F6` 배경, `#6B7280` 텍스트) |
| 처리중 | 노랑 (`#FEF9C3` 배경, `#A16207` 텍스트) |
| 완료 | 초록 (`#DCFCE7` 배경, `#15803D` 텍스트) |

### 3.9 관리자 답변 (신규)


- **관리자 모드**에서 각 카드 하단에 접이식(collapsible) 답변 패널을 표시한다.
- 패널은 "답변 작성" 토글 버튼으로 열고 닫는다.
- 패널 내 `<textarea>` + "저장" 버튼으로 답변을 등록·수정한다.
- 저장 시 `reply` 텍스트와 `repliedAt` 타임스탬프를 함께 기록한다.
- 답변이 존재하면 **일반 사용자 뷰**에서도 카드 하단에 읽기 전용으로 표시한다.
  - 표시 형식: 회색 구분선 아래 "담당자 답변 · N분 전" 레이블 + 답변 내용.

### 3.10 탭 내비게이션 (신규)

- 헤더 바로 아래에 탭 바를 고정(sticky)하여 표시한다.
- 탭 구성:

| 탭 | 표시 조건 | 설명 |
|---|---|---|
| 문의 등록 | 항상 표시 | 문의 등록 폼 패널 |
| 문의 목록 | 항상 표시 | 문의 목록·필터·엑셀 툴바 패널 |
| 관리자 설정 | **관리자 모드에서만** 표시 | 엑셀 가져오기/내보내기 + Anthropic API 키 설정 패널 |

- 활성 탭: 하단 2px 파란 밑줄(`border-primary`), `primary` 텍스트 색상.
- 비활성 탭: 투명 밑줄, 회색 텍스트. hover 시 회색 밑줄.
- 접근성: `role="tab"`, `aria-selected`, `role="tabpanel"`, `aria-labelledby` 적용.
- 관리자 로그인 시 "문의 목록" 탭으로 자동 전환.
- 로그아웃 시 "문의 등록" 탭으로 자동 전환. 관리자 전용 탭은 숨김 처리.

### 3.11 LLM 자동 답변 생성 (신규)

관리자 모드에서 Claude API를 호출하여 문의 내용을 분석하고 답변을 자동 생성·즉시 저장한다.

#### 3.11.1 API 키 설정

- **관리자 설정** 탭에 Anthropic API 키 입력 섹션을 추가한다.

| UI 요소 | 설명 |
|---|---|
| `<input type="password">` | API 키 입력 (`sk-ant-...`) |
| "저장" 버튼 | `localStorage` 키 `bipa-portal:anthropic-key`에 저장 |
| "삭제" 버튼 | 저장된 키 삭제 (초기화) |
| 상태 표시 | 저장된 키가 있으면 "✔ API 키 등록됨", 없으면 "API 키를 입력해주세요" |

- 키는 `localStorage`에 평문 저장한다 (교육·시범용).
- API 키가 등록되지 않은 경우 "AI 답변 생성" 버튼을 비활성화(disabled)하고 tooltip으로 "관리자 설정에서 API 키를 먼저 등록하세요."를 표시한다.

#### 3.11.2 AI 답변 생성 버튼

- **관리자 모드**의 각 카드 답변 패널 내 `<textarea>` 상단에 "✨ AI 답변 생성" 버튼을 추가한다.
- 버튼 클릭 흐름:

```
버튼 클릭
  → 버튼을 로딩 상태("생성 중...")로 전환, disabled 처리
  → Anthropic API 호출 (아래 §3.11.3 참고)
  → 성공: 생성된 텍스트를 reply 필드에 저장 + repliedAt = 현재 시각 기록
         + 목록·카드 즉시 갱신 + 버튼 원상 복귀
  → 실패: alert("AI 답변 생성에 실패했습니다. API 키와 네트워크를 확인해주세요.")
         + 버튼 원상 복귀 (기존 reply 값 유지)
```

#### 3.11.3 API 호출 명세

| 항목 | 값 |
|---|---|
| 엔드포인트 | `https://api.anthropic.com/v1/messages` |
| 메서드 | POST |
| 모델 | `claude-haiku-4-5-20251001` |
| `max_tokens` | 512 |
| 헤더 | `x-api-key: <저장된 키>`, `anthropic-version: 2023-06-01`, `content-type: application/json` |

> **CORS 참고:** 브라우저에서 `api.anthropic.com`으로 직접 요청 시 CORS 제한이 발생할 수 있다. 교육·시범 환경에서는 브라우저의 CORS 정책에 따라 동작 여부가 달라지므로, 실운영 시에는 백엔드 프록시를 통해 API 키를 보호하는 것을 권장한다.

**시스템 프롬프트:**

```
당신은 부산정보산업진흥원(BIPA) AI·DX 지원사업 담당 직원입니다.
아래 시민·기업의 문의에 대해 친절하고 전문적인 한국어 답변을 작성해주세요.
- 200자 이내로 간결하게 작성합니다.
- 구체적인 안내가 어려운 경우 BIPA 홈페이지(www.bipa.or.kr) 또는 대표번호(051-749-9300) 문의를 안내합니다.
- 정중하고 공손한 어투를 사용합니다.
```

**사용자 메시지 구성:**

```
카테고리: {category}
제목: {title}
문의 내용: {content}
```

#### 3.11.4 UI/UX 명세

| 상태 | 버튼 레이블 | 버튼 스타일 |
|---|---|---|
| 기본 (키 등록됨) | ✨ AI 답변 생성 | `accent` 계열 테두리 버튼 |
| 생성 중 | ⏳ 생성 중... | 회색, disabled |
| 키 미등록 | ✨ AI 답변 생성 | 회색, disabled, tooltip |

- 버튼은 `<textarea>` 바로 위, "저장" 버튼과 같은 행(row) 좌측에 배치한다.
- 생성 완료 후 `<textarea>`에 결과가 자동으로 채워지고 즉시 저장된다 (별도 "저장" 버튼 클릭 불필요).

#### 3.11.5 보안 및 제약

| 항목 | 내용 |
|---|---|
| API 키 노출 | `localStorage`에 평문 저장. 교육·시범 목적에 한해 허용. 운영 환경에서는 서버 프록시 필요 |
| 요청 빈도 | 건당 1회 호출. 별도 rate-limit 로직 없음 |
| 비용 | 관리자가 직접 소유한 Anthropic API 키 사용. 비용은 키 소유자 부담 |
| 오프라인 | 네트워크 없는 환경에서는 API 호출 실패 처리 |

---

## 4. 데이터 모델

```json
{
  "id": 1,
  "category": "AI·DX 솔루션 도입",
  "title": "문의 제목",
  "name": "홍길동",
  "content": "문의 내용",
  "ts": 1748573600000,
  "status": "접수됨",
  "reply": "",
  "repliedAt": null
}
```

| 필드 | 타입 | 설명 |
|---|---|---|
| `id` | number | 자동 증가 정수 ID |
| `category` | string | 7개 카테고리 중 하나 |
| `title` | string | 문의 제목 |
| `name` | string | 작성자 이름 |
| `content` | string | 문의 내용 |
| `ts` | number | Unix timestamp (밀리초) |
| `status` | `"접수됨" \| "처리중" \| "완료"` | 문의 처리 상태. 기본값 `"접수됨"` |
| `reply` | string | 관리자 답변 텍스트. 기본값 `""` |
| `repliedAt` | number \| null | 답변 등록 Unix timestamp (밀리초). 기본값 `null` |

**저장소:** `localStorage`, 키 = `bipa-portal:inquiries`  
**초기 데이터:** 로컬스토리지에 데이터가 없을 경우 5건의 샘플 데이터로 초기화.  
**ID 시퀀스:** 앱 로드 시 기존 최대 ID + 1에서 시작 (최솟값 100).  
**인증 상태:** `sessionStorage`, 키 = `bipa-portal:admin` (값: `"true"`)  
**Anthropic API 키:** `localStorage`, 키 = `bipa-portal:anthropic-key` (평문 문자열)

---

## 5. 샘플 데이터 (5건)

| 카테고리 | 제목 | 이름 | 기준 시간 | 상태 |
|---|---|---|---|---|
| AI·DX 솔루션 도입 | AI 솔루션 실증 비용 지원 가능한가요? | 김도윤 | 현재 -2분 | 접수됨 |
| 데이터·클라우드 바우처 | 데이터 바우처 신청 기간이 언제인가요? | 박서연 | 현재 -15분 | 처리중 |
| 스타트업 입주·창업 | 센텀 스타트업 입주 자격 문의 | 이준혁 | 현재 -47분 | 완료 |
| 게임·콘텐츠 제작 지원 | Bu:Star 게임 제작비 신청 절차 | 최유진 | 현재 -2시간 | 접수됨 |
| 교육·인재양성 신청 | AI 활용 교육과정 일정 안내 부탁드립니다 | 정민서 | 현재 -5시간 | 접수됨 |

- 3번째 항목(이준혁, 완료)에는 샘플 답변을 포함한다.
  - `reply`: `"센텀 스타트업 캠퍼스 입주 자격 요건을 안내드립니다. 창업 후 7년 이내 기업이면 신청 가능하며, 자세한 내용은 BIPA 홈페이지를 참고해 주세요."`
  - `repliedAt`: 현재 -30분

---

## 6. UI/UX 명세

### 레이아웃

- 최대 너비 `max-w-3xl` (768px), 중앙 정렬.
- 섹션 구성: 헤더 → 문의 등록 폼 → 문의 목록 → 푸터.

### 디자인 시스템 (Tailwind CSS)

| 토큰 | 값 |
|---|---|
| `primary` | `#1E40AF` (진한 파랑) |
| `accent` | `#0EA5E9` (밝은 파랑) |
| `surface` | `#F8FAFC` (배경 회색) |
| `borderRadius.card` | `8px` |
| 폰트 | Noto Sans KR (Google Fonts) |

### 컴포넌트 명세

| 컴포넌트 | 설명 |
|---|---|
| **헤더** | `primary` 배경, 흰 텍스트. 원형 아바타(「부」), 기관명, 제목, 부제. 우측에 "관리자" 버튼 (비로그인) 또는 "로그아웃" 버튼 (로그인) 표시 |
| **로그인 모달** | 중앙 오버레이 모달. 비밀번호 `<input type="password">` + "확인" 버튼 + 인라인 오류 메시지 영역. ESC 또는 배경 클릭으로 닫기 |
| **카드** | 흰 배경, `border-gray-200`, `shadow-sm`, `rounded-card(8px)` |
| **카테고리 뱃지** | `#DBEAFE` 배경, `#1E40AF` 텍스트, pill 형태 |
| **상태 뱃지** | 상태별 색상 (§3.8 참고), pill 형태. 관리자 모드에서는 `<select>`로 교체 |
| **필터 칩** | 비활성: 흰 배경 + 테두리. 활성: `primary` 배경 + 흰 텍스트. hover: 연파랑 |
| **삭제 버튼** | 회색 테두리, hover 시 빨간색 전환. **관리자 모드에서만 표시** |
| **엑셀 툴바** | **관리자 모드에서만 표시** |
| **등록 버튼** | `primary` 배경, hover 시 `blue-900` |
| **답변 패널** | 관리자 모드: 카드 하단 접이식 패널 (회색 구분선, textarea, 저장 버튼). 일반 모드: 답변 존재 시 읽기 전용 블록 표시 |

### 보안

- 모든 사용자 입력은 HTML 특수문자 이스케이프(`&`, `<`, `>`, `"`) 처리 후 렌더링.

---

## 7. 기술 스택

| 항목 | 기술 |
|---|---|
| 마크업 | HTML5 (단일 파일) |
| 스타일링 | Tailwind CSS v3 (CDN) |
| 엑셀 처리 | SheetJS (xlsx 0.20.3, CDN) |
| LLM API | Anthropic Messages API (직접 fetch 호출) |
| 폰트 | Google Fonts – Noto Sans KR |
| 데이터 저장 | Browser localStorage |
| 인증 상태 | Browser sessionStorage |
| 빌드 도구 | 없음 (순수 HTML) |

---

## 8. 비기능 요구사항

| 항목 | 요구사항 |
|---|---|
| 반응형 | 모바일 ~ 데스크탑 대응 (Tailwind 반응형 유틸리티 활용) |
| 오프라인 동작 | CDN 의존 외에 별도 서버 불필요, localStorage 기반 완전 동작 |
| 접근성 | `lang="ko"` 선언, label-input 연결, 시맨틱 마크업(header, main, section, footer) |
| 성능 | 목록 30초 자동 갱신 (상대시각 업데이트 목적) |
| 브라우저 지원 | 최신 Chrome, Safari, Edge (localStorage, FileReader API 지원 필요) |
| 인증 보안 | 교육·시범용 클라이언트 인증. 소스 공개 환경이므로 운영 보안으로 사용 불가 |

---

## 9. 추후 확장 고려사항 (현재 미구현)

- 서버 기반 데이터베이스 연동 (현재 localStorage 한계 극복)
- 실명 인증 기반 본인 확인
- 서버 사이드 관리자 인증 (현재 클라이언트 하드코딩 방식 대체)
- 이메일 알림 연동
- 검색 기능 (키워드 검색)
- 페이지네이션 (대량 문의 대응)
- 다국어 지원 (영어)
- 관리자 상태 필터 (접수됨/처리중/완료 별도 필터 칩)
